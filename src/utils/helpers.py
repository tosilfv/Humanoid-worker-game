def direction_term(dir_m, dir_n):
    """Get either 1 or -1 depending on direction."""
    return (-1 * dir_m + dir_n)

def print_message(message):
    """Prints message."""
    print(message)
