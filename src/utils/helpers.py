def direction_term(dir_m: int, dir_n: int) -> int:
    """Get either 1 or -1 depending on direction."""
    return (-1 * dir_m + dir_n)

def print_message(message: str) -> None:
    """Prints message."""
    print(message)
    return
